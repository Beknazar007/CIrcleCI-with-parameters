
version: 2.1
jobs:
  build:
    docker:
      - image: ruby:2.4.0-jessie
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'

      - run:
          
          name: Trigger docker remotely
          command: |  
            curl --location --request POST  -u ${CIRCLE_API_USER_TOKEN}  https://circleci.com/api/v1.1/project/github/Nurmukhamed02/trigger-circleci/main
workflow:
  jobs:
    - build
